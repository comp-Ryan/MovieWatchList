{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": ""
  },
  "license": "ISC",
  "author": "Ryan Lee",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "build:ui": "shx rm -rf dist && cd ../frontend && npm run build && shx cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit && git push",
    "deploy:render": "npm run build:ui && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "shx": "^0.4.0"
  }
}
